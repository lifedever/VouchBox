import SwiftUI

@main
struct VouchBoxApp: App {
    var body: some Scene {
        WindowGroup("VouchBox") {
            Text("Loading…")
                .frame(minWidth: 800, minHeight: 500)
        }
    }
}
